"""Binary search is something like firs the array or list will be divided into two parts and find elements 
in both parts and if its present in first part then again the first part should be divided into 
two parts and so on until the element will not be found"""

def Binary_search(target, lis):
    
    for i in range(len(lis)):
        pass